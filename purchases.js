(async()=>{const A=window.Akram,esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),out=document.getElementById('purchases'),status=document.getElementById('status');async function refresh(){status.textContent='Loading / Sug…';try{if(!A.enabled)throw Error('Setup not complete / Rakibiddu ma dhammaan');if(!await A.user()){out.innerHTML='<a class="admin-link" href="account.html?view=signin">Sign in / Soo gal</a>';status.textContent='Sign in to see purchases / Soo gal si aad u aragto buugaagtaada.';return}const orders=await A.orders(),rows=await A.rows();out.innerHTML=orders.map(o=>{const b=rows.find(r=>r.id===o.book_id);return `<article class="order-card"><h3>${esc(b?.data.title||o.book_id)}</h3><p>$${Number(o.amount).toFixed(2)} · ${esc(o.method)} · ${esc(o.reference)}</p><p>${o.status==='approved'?'Approved / Waa la ansixiyey':o.status==='pending'?'Awaiting verification. Do not pay again. / Sug hubinta; mar kale lacag ha bixin.':'Rejected. Contact the owner before paying again. / Waa la diiday; la xiriir maamulka ka hor intaadan lacag kale bixin.'}</p>${o.status==='approved'?`<a class="admin-link" href="read.html?book=${encodeURIComponent(o.book_id)}&start=1">Read now / Akhri →</a>`:`<a href="index.html?offer=${encodeURIComponent(o.book_id)}">View details / Eeg faahfaahin →</a>`}</article>`}).join('')||'<p>No purchases yet / Weli ma iibsan buug.</p>';status.textContent=''}catch(e){status.textContent=e.message}}document.getElementById('refresh').onclick=refresh;await refresh()})();
